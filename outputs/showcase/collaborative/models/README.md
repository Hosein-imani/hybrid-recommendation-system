This directory contains generated model artifacts.

Files generated after training:

- svd_model.pkl
- trainset.pkl
- metadata.json

Large binary files are intentionally excluded from Git to keep the repository lightweight.

Run:

scripts/collaborative/run_svd.py

to regenerate these artifacts.